app.controller("driver_information_details_controller", ["$scope", "$host", "$stateParams", "_basic", function ($scope, $host, $stateParams, _basic) {

    var driverId = $stateParams.driverId;

    // 获取数据
    $scope.queryData = function () {

    };
    $scope.queryData();
}]);